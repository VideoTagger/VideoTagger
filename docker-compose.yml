services:
  videotagger:
    build: ./docker/linux
    image: videotagger:latest
    container_name: videotagger
    working_dir: /app
    volumes:
      - .:/app
      - build_cache:/app/build
    command: >
      bash -c "
      cmake --preset=linux-x64-shipping &&
      cmake --build --target install --preset=linux-x64-shipping
      "
volumes:
  build_cache:
      name: videotagger-build-cache
